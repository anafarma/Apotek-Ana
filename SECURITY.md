# Security Policy

Do not commit credentials, spreadsheet IDs that are intended to be secret, deployment tokens, API keys, OAuth secrets, or customer personal data.

Report security issues privately to the repository owner rather than opening a public issue with sensitive details.

The application must treat Google Apps Script deployment credentials and environment configuration as secrets. Production data must never be copied into source control.